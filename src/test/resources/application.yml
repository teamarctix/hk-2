spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/todo-dev-test
    username: user
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true